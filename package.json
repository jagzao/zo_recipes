{
  "name": "kitcheneye",
  "version": "0.1.0",
  "description": "KitchenEye - Smart Kitchen Monitoring PWA",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:worker": "wrangler dev",
    "dev:pages": "wrangler pages dev frontend/public --compatibility-date=2024-01-01",
    "build": "npm run build:frontend && npm run build:worker",
    "build:frontend": "vite build",
    "build:worker": "esbuild workers/api/index.ts --bundle --outdir=dist/workers/api --format=esm --platform=node --target=es2020",
    "preview": "vite preview",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply kitcheneye-db",
    "db:create": "wrangler d1 create kitcheneye-db",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:chromium": "playwright test --project=chromium",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "nanostores": "^1.0.1",
    "web-vitals": "^5.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@vitest/coverage-v8": "^1.1.0",
    "esbuild": "^0.19.11",
    "eslint": "^8.56.0",
    "playwright": "^1.56.1",
    "terser": "^5.44.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0",
    "wrangler": "^3.22.1"
  }
}
